<Window x:Class="ProbaWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProbaWPF"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <DockPanel>
        <TextBox DockPanel.Dock="Top" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />
        <Slider DockPanel.Dock="Top" Maximum="700" Value="{Binding Position}"></Slider>
        <TextBox DockPanel.Dock="Top" Text="{Binding Position}" />
        <Canvas>
            <Button DockPanel.Dock="Top" Content="Ok" Canvas.Left="{Binding Position}" Canvas.Top="20"/>
        </Canvas>
    </DockPanel>
</Window>
